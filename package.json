{
  "name": "anvil",
  "version": "0.0.0",
  "private": true,
  "description": "Anvil — an autonomous, self-healing, self-learning coding harness.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "smoke": "pnpm build && tsc --noEmit -p smoke/tsconfig.json && tsx smoke/smoke.ts",
    "smoke:live": "pnpm build && tsc --noEmit -p smoke/tsconfig.json && tsx smoke/live.ts",
    "smoke:build": "pnpm build && tsc --noEmit -p smoke/tsconfig.json && tsx smoke/build.ts",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "link:cli": "cd packages/cli && pnpm link --global",
    "link:mcp": "cd packages/mcp-server && pnpm link --global",
    "link:service": "cd packages/service && pnpm link --global",
    "link:global": "pnpm link:cli && pnpm link:mcp && pnpm link:service",
    "unlink:cli": "cd packages/cli && pnpm unlink --global",
    "unlink:mcp": "cd packages/mcp-server && pnpm unlink --global",
    "unlink:service": "cd packages/service && pnpm unlink --global",
    "unlink:global": "pnpm unlink:cli && pnpm unlink:mcp && pnpm unlink:service",
    "setup-anvil": "pnpm install && pnpm build && pnpm link:global"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^2.1.9",
    "eslint": "^9.13.0",
    "globals": "^17.6.0",
    "prettier": "^3.3.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.14.0",
    "vitest": "^2.1.5"
  }
}
