/**
 * @anvil/shared — shared types and zod schemas for the Anvil harness.
 *
 * Phase 1: scaffold only. The real schemas — Task, PlanNode, MemoryFact, Job,
 * ToolCall, Event, ApprovalToken — land alongside the engine subsystems in
 * Phase 2 and beyond. See ../../docs/architecture.md.
 */

export const ANVIL_SHARED_VERSION = "0.0.0";
