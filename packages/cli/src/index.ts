#!/usr/bin/env node
/**
 * anvil — command-line interface.
 *
 * Phase 1: scaffold only. Commands (`anvil build`, `anvil ingest`,
 * `anvil memory`, `anvil init`, …) land in Milestone C once the core engine
 * exists. See ../../docs/architecture.md.
 */

function main(): void {
  console.log("anvil v0.0.0 — harness core scaffold (Phase 1). Commands land in Milestone C.");
}

main();
