/**
 * @anvil/service — HTTP + queue daemon wrapping the Anvil core.
 *
 * Phase 1: scaffold only. Long-running builds run here as async jobs, with
 * progress streamed back to callers. The HTTP API lands in Milestone C.
 */

export const ANVIL_SERVICE_VERSION = "0.0.0";
