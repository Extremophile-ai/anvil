{
  "name": "@anvil/service",
  "version": "0.0.0",
  "private": true,
  "description": "HTTP + queue daemon wrapping the Anvil core for long-running async builds.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "anvil-service": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@anvil/core": "workspace:*",
    "@anvil/shared": "workspace:*",
    "zod": "^4.4.3"
  }
}
